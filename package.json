{
  "name": "tab-monitor-alerter",
  "version": "1.0.0",
  "description": "Chrome extension that monitors tabs for title changes and web notifications, then alerts the user.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "playwright test",
    "test:integration:headed": "playwright test --headed",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@playwright/test": "^1.44.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "vitest": "^4.1.8"
  }
}
